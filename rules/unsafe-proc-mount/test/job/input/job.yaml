apiVersion: batch/v1
kind: Job
metadata:
  name: unsafe-proc-mount-job
spec:
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: worker
          image: nginx
          securityContext:
            procMount: Unmasked
